class Main inherits IO {
    main(): IO {{
        let s: String <- in_string() in out_string(s);
        let s: String <- in_string() in out_string(s);
        let s: String <- in_string() in out_string(s);
    }};
};
