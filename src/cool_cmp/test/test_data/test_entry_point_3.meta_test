"{\"name\": \"test_entry_point_3\", \"result\": 30, \"errors\": [\"The program must define a 'Main' type\"]}"


class NoMain{

    main() : Int {
        {
            30;
        }
    };

};