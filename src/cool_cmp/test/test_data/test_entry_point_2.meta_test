"{\"name\": \"test_entry_point_2\", \"result\": 30, \"errors\": [\"Method 'main' is not defined locally in 'Main' with 0 params. The program must define a 'main' method with no parameters in the 'Main' class\"]}"

class M{
    main() : Int {
        {
            30;
        }
    };
};

class Main inherits M{

};