class Main inherits IO {
    out: Int <- {
    self.out_string("2 is trivially prime.\n");
2;
};

    testee: Int <- out;

    divisor: Int;

    stop: Int <- 500;

    m: Object <- while true loop
     {
        testee <-         testee + 1;
        divisor <- 2;
        while if testee < divisor * divisor
then
false
else
    if testee - divisor * testee / divisor = 0
    then
false
    else
true
    fi
fi loop
             divisor <-             divisor + 1
        pool;
        if testee < divisor * divisor
        then
            {
                out <-                 testee;
                self.out_int(out);
                self.out_string(" is prime.\n");
            }
        else
0
        fi;
        if stop <= testee
        then
            "halt".abort()
        else
"continue"
        fi;
    }
pool;

    __init__ (): Main {
        {
            out <-             {
                self.out_string("2 is trivially prime.\n");
2;
            };
            testee <-             out;
            divisor <- 0;
            stop <- 500;
            m <-             while true loop
                 {
                    testee <-                     testee + 1;
                    divisor <- 2;
                    while if testee < divisor * divisor
then
false
else
    if testee - divisor * testee / divisor = 0
    then
false
    else
true
    fi
fi loop
                         divisor <-                         divisor + 1
                    pool;
                    if testee < divisor * divisor
                    then
                        {
                            out <-                             testee;
                            self.out_int(out);
                            self.out_string(" is prime.\n");
                        }
                    else
0
                    fi;
                    if stop <= testee
                    then
                        "halt".abort()
                    else
"continue"
                    fi;
                }
            pool;
            self;
        }
    };

    main (): Int {
0
    };
}