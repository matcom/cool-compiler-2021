# Template for < operator.
# Args:
#   $a0: pointer to Int instance
#   $a1: pointer to Int instance
# Returns:
#   $v0: 1 if the Int in $a0 is less than the Int in $a1, false in other case.
less_than:
    lw      $t0, 4($a0)
    lw      $t1, 4($a1)
    blt     $t0, $t1, first_smaller
    li      $v0, 0
    j       end_less_than
first_smaller:
    li      $v0, 1
end_less_than:
    jr      $ra