malloc:
    subu    $sp, $sp, 24        # $sp = $sp - size, allocate memory for the frame
    sw      $fp, 4($sp)         # save $ra
    addu    $fp, $sp, 20        # $fp = $fp + (size - 4)

    li      $v0, 9
    syscall

    lw      $fp, 4($sp)
    addu    $sp, $sp, 24
    jr      $ra