# Template for <= operator.
# Args:
#   $a0: pointer to Int instance
#   $a1: pointer to Int instance
# Returns:
#   $v0: 1 if the Int in $a0 is less than or equal to the Int in $a1, false in other case.
less_equal:
    lw      $t0, 4($a0)
    lw      $t1, 4($a1)
    ble     $t0, $t1, first_smaller_or_equal
    li      $v0, 0
    j       end_less_equal
first_smaller_or_equal:
    li      $v0, 1
end_less_equal:
    jr      $ra