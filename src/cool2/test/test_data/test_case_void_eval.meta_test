"{\"name\": \"test_case_void_eval\", \"result\": 50, \"errors\": [\"Void is not a valid type. Line: 4 Column: 27\"]}"

class Main inherits Object {
   main():Object {
       {
           a:Object <- case while false loop 1 pool of
           -- a:Bool => if a then true else false fi;
           b:Int => if b = 60 then 50 else 0 fi;
           c:Object => c;
           esac;
           a;
       }
   };
}
